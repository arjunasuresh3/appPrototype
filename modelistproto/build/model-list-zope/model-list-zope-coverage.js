if (typeof _yuitest_coverage == "undefined"){
    _yuitest_coverage = {};
    _yuitest_coverline = function(src, line){
        var coverage = _yuitest_coverage[src];
        if (!coverage.lines[line]){
            coverage.calledLines++;
        }
        coverage.lines[line]++;
    };
    _yuitest_coverfunc = function(src, name, line){
        var coverage = _yuitest_coverage[src],
            funcId = name + ":" + line;
        if (!coverage.functions[funcId]){
            coverage.calledFunctions++;
        }
        coverage.functions[funcId]++;
    };
}
_yuitest_coverage["build/model-list-zope/model-list-zope.js"] = {
    lines: {},
    functions: {},
    coveredLines: 0,
    calledLines: 0,
    coveredFunctions: 0,
    calledFunctions: 0,
    path: "build/model-list-zope/model-list-zope.js",
    code: []
};
_yuitest_coverage["build/model-list-zope/model-list-zope.js"].code=["YUI.add('model-list-zope', function (Y, NAME) {","","/**","Provides an API for managing an ordered list of Model instances.","","@module app","@submodule model-list","@since 3.4.0","**/","","/**","Provides an API for managing an ordered list of Model instances.","","In addition to providing convenient `add`, `create`, `reset`, and `remove`","methods for managing the models in the list, ModelLists are also bubble targets","for events on the model instances they contain. This means, for example, that","you can add several models to a list, and then subscribe to the `*:change` event","on the list to be notified whenever any model in the list changes.","","ModelLists also maintain sort order efficiently as models are added and removed,","based on a custom `comparator` function you may define (if no comparator is","defined, models are sorted in insertion order).","","@class ModelList","@extends Base","@uses ArrayList","@constructor","@param {Object} config Config options.","    @param {Model|Model[]|ModelList|Object|Object[]} config.items Model","        instance, array of model instances, or ModelList to add to this list on","        init. The `add` event will not be fired for models added on init.","@since 3.4.0","**/","","var AttrProto = Y.Attribute.prototype,","    Lang      = Y.Lang,","    YArray    = Y.Array,","","    /**","    Fired when a model is added to the list.","","    Listen to the `on` phase of this event to be notified before a model is","    added to the list. Calling `e.preventDefault()` during the `on` phase will","    prevent the model from being added.","","    Listen to the `after` phase of this event to be notified after a model has","    been added to the list.","","    @event add","    @param {Model} model The model being added.","    @param {Number} index The index at which the model will be added.","    @preventable _defAddFn","    **/","    EVT_ADD = 'add',","","    /**","    Fired when a model is created or updated via the `create()` method, but","    before the model is actually saved or added to the list. The `add` event","    will be fired after the model has been saved and added to the list.","","    @event create","    @param {Model} model The model being created/updated.","    @since 3.5.0","    **/","    EVT_CREATE = 'create',","","    /**","    Fired when an error occurs, such as when an attempt is made to add a","    duplicate model to the list, or when a sync layer response can't be parsed.","","    @event error","    @param {Any} error Error message, object, or exception generated by the","      error. Calling `toString()` on this should result in a meaningful error","      message.","    @param {String} src Source of the error. May be one of the following (or any","      custom error source defined by a ModelList subclass):","","      * `add`: Error while adding a model (probably because it's already in the","         list and can't be added again). The model in question will be provided","         as the `model` property on the event facade.","      * `parse`: An error parsing a JSON response. The response in question will","         be provided as the `response` property on the event facade.","      * `remove`: Error while removing a model (probably because it isn't in the","        list and can't be removed). The model in question will be provided as","        the `model` property on the event facade.","    **/","    EVT_ERROR = 'error',","","    /**","    Fired after models are loaded from a sync layer.","","    @event load","    @param {Object} parsed The parsed version of the sync layer's response to","        the load request.","    @param {Mixed} response The sync layer's raw, unparsed response to the load","        request.","    @since 3.5.0","    **/","    EVT_LOAD = 'load',","","    /**","    Fired when a model is removed from the list.","","    Listen to the `on` phase of this event to be notified before a model is","    removed from the list. Calling `e.preventDefault()` during the `on` phase","    will prevent the model from being removed.","","    Listen to the `after` phase of this event to be notified after a model has","    been removed from the list.","","    @event remove","    @param {Model} model The model being removed.","    @param {Number} index The index of the model being removed.","    @preventable _defRemoveFn","    **/","    EVT_REMOVE = 'remove',","","    /**","    Fired when the list is completely reset via the `reset()` method or sorted","    via the `sort()` method.","","    Listen to the `on` phase of this event to be notified before the list is","    reset. Calling `e.preventDefault()` during the `on` phase will prevent","    the list from being reset.","","    Listen to the `after` phase of this event to be notified after the list has","    been reset.","","    @event reset","    @param {Model[]} models Array of the list's new models after the reset.","    @param {String} src Source of the event. May be either `'reset'` or","      `'sort'`.","    @preventable _defResetFn","    **/","    EVT_RESET = 'reset';","","    /**","    Fired after model attributes are saved to a sync layer.","","    @event save","    @param {Object} [parsed] The parsed version of the sync layer's response to","        the save request, if there was a response.","    @param {any} [response] The sync layer's raw, unparsed response to the save","        request, if there was one.","    @since 3.5.0","    **/","    EVT_SAVE = 'save';","","function ModelListZope() {","    ModelListZope.superclass.constructor.apply(this, arguments);","}","","Y.ModelListZope = Y.extend(ModelListZope, Y.Base, {","    // -- Public Properties ----------------------------------------------------","","    /**","    The `Model` class or subclass of the models in this list.","","    The class specified here will be used to create model instances","    automatically based on attribute hashes passed to the `add()`, `create()`,","    and `reset()` methods.","","    You may specify the class as an actual class reference or as a string that","    resolves to a class reference at runtime (the latter can be useful if the","    specified class will be loaded lazily).","","    @property model","    @type Model|String","    @default Y.Model","    **/","    model: Y.Model,","","    // -- Protected Properties -------------------------------------------------","","    /**","    Total hack to allow us to identify ModelList instances without using","    `instanceof`, which won't work when the instance was created in another","    window or YUI sandbox.","","    @property _isYUIModelList","    @type Boolean","    @default true","    @protected","    @since 3.5.0","    **/","    _isYUIModelList: true,","","    // -- Lifecycle Methods ----------------------------------------------------","    initializer: function (config) {","        config || (config = {});","","        var model = this.model = config.model || this.model;","","        if (typeof model === 'string') {","            // Look for a namespaced Model class on `Y`.","            this.model = Y.Object.getValue(Y, model.split('.'));","","            if (!this.model) {","                Y.error('ModelList: Model class not found: ' + model);","            }","        }","","        this.publish(EVT_ADD,    {defaultFn: this._defAddFn});","        this.publish(EVT_RESET,  {defaultFn: this._defResetFn});","        this.publish(EVT_REMOVE, {defaultFn: this._defRemoveFn});","","        this.after('*:idChange', this._afterIdChange);","","        this._clear();","","        if (config.items) {","            this.add(config.items, {silent: true});","        }","    },","","    destructor: function () {","        this._clear();","    },","","    // -- Public Methods -------------------------------------------------------","","    /**","    Adds the specified model or array of models to this list. You may also pass","    another ModelList instance, in which case all the models in that list will","    be added to this one as well.","","    @example","","        // Add a single model instance.","        list.add(new Model({foo: 'bar'}));","","        // Add a single model, creating a new instance automatically.","        list.add({foo: 'bar'});","","        // Add multiple models, creating new instances automatically.","        list.add([","            {foo: 'bar'},","            {baz: 'quux'}","        ]);","","        // Add all the models in another ModelList instance.","        list.add(otherList);","","    @method add","    @param {Model|Model[]|ModelList|Object|Object[]} models Model or array of","        models to add. May be existing model instances or hashes of model","        attributes, in which case new model instances will be created from the","        hashes. You may also pass a ModelList instance to add all the models it","        contains.","    @param {Object} [options] Data to be mixed into the event facade of the","        `add` event(s) for the added models.","","        @param {Number} [options.index] Index at which to insert the added","            models. If not specified, the models will automatically be inserted","            in the appropriate place according to the current sort order as","            dictated by the `comparator()` method, if any.","        @param {Boolean} [options.silent=false] If `true`, no `add` event(s)","            will be fired.","","    @return {Model|Model[]} Added model or array of added models.","    **/","    add: function (models, options) {","        var isList = models._isYUIModelList;","","        if (isList || Lang.isArray(models)) {","            return YArray.map(isList ? models.toArray() : models, function (model, index) {","                var modelOptions = options || {};","","                // When an explicit insertion index is specified, ensure that","                // the index is increased by one for each subsequent item in the","                // array.","                if ('index' in modelOptions) {","                    modelOptions = Y.merge(modelOptions, {","                        index: modelOptions.index + index","                    });","                }","","                return this._add(model, modelOptions);","            }, this);","        } else {","            return this._add(models, options);","        }","    },","","    /**","    Define this method to provide a function that takes a model as a parameter","    and returns a value by which that model should be sorted relative to other","    models in this list.","","    By default, no comparator is defined, meaning that models will not be sorted","    (they'll be stored in the order they're added).","","    @example","        var list = new Y.ModelList({model: Y.Model});","","        list.comparator = function (model) {","            return model.get('id'); // Sort models by id.","        };","","    @method comparator","    @param {Model} model Model being sorted.","    @return {Number|String} Value by which the model should be sorted relative","      to other models in this list.","    **/","","    // comparator is not defined by default","","    /**","    Creates or updates the specified model on the server, then adds it to this","    list if the server indicates success.","","    @method create","    @param {Model|Object} model Model to create. May be an existing model","      instance or a hash of model attributes, in which case a new model instance","      will be created from the hash.","    @param {Object} [options] Options to be passed to the model's `sync()` and","        `set()` methods and mixed into the `create` and `add` event facades.","      @param {Boolean} [options.silent=false] If `true`, no `add` event(s) will","          be fired.","    @param {Function} [callback] Called when the sync operation finishes.","      @param {Error} callback.err If an error occurred, this parameter will","        contain the error. If the sync operation succeeded, _err_ will be","        falsy.","      @param {Any} callback.response The server's response.","    @return {Model} Created model.","    **/","    create: function (model, options, callback) {","        var self = this;","","        // Allow callback as second arg.","        if (typeof options === 'function') {","            callback = options;","            options  = {};","        }","","        options || (options = {});","","        if (!model._isYUIModel) {","            model = new this.model(model);","        }","","        self.fire(EVT_CREATE, Y.merge(options, {","            model: model","        }));","","        return model.save(options, function (err) {","            if (!err) {","                self.add(model, options);","            }","","            callback && callback.apply(null, arguments);","        });","    },","","    /**","    Executes the supplied function on each model in this list.","","    By default, the callback function's `this` object will refer to the model","    currently being iterated. Specify a `thisObj` to override the `this` object","    if desired.","","    Note: Iteration is performed on a copy of the internal array of models, so","    it's safe to delete a model from the list during iteration.","","    @method each","    @param {Function} callback Function to execute on each model.","        @param {Model} callback.model Model instance.","        @param {Number} callback.index Index of the current model.","        @param {ModelList} callback.list The ModelList being iterated.","    @param {Object} [thisObj] Object to use as the `this` object when executing","        the callback.","    @chainable","    @since 3.6.0","    **/","    each: function (callback, thisObj) {","        var items = this._items.concat(),","            i, item, len;","","        for (i = 0, len = items.length; i < len; i++) {","            item = items[i];","            callback.call(thisObj || item, item, i, this);","        }","","        return this;","    },","","    /**","    Executes the supplied function on each model in this list. Returns an array","    containing the models for which the supplied function returned a truthy","    value.","","    The callback function's `this` object will refer to this ModelList. Use","    `Y.bind()` to bind the `this` object to another object if desired.","","    @example","","        // Get an array containing only the models whose \"enabled\" attribute is","        // truthy.","        var filtered = list.filter(function (model) {","            return model.get('enabled');","        });","","        // Get a new ModelList containing only the models whose \"enabled\"","        // attribute is truthy.","        var filteredList = list.filter({asList: true}, function (model) {","            return model.get('enabled');","        });","","    @method filter","    @param {Object} [options] Filter options.","        @param {Boolean} [options.asList=false] If truthy, results will be","            returned as a new ModelList instance rather than as an array.","","    @param {Function} callback Function to execute on each model.","        @param {Model} callback.model Model instance.","        @param {Number} callback.index Index of the current model.","        @param {ModelList} callback.list The ModelList being filtered.","","    @return {Array|ModelList} Array of models for which the callback function","        returned a truthy value (empty if it never returned a truthy value). If","        the `options.asList` option is truthy, a new ModelList instance will be","        returned instead of an array.","    @since 3.5.0","    */","    filter: function (options, callback) {","        var filtered = [],","            items    = this._items,","            i, item, len, list;","","        // Allow options as first arg.","        if (typeof options === 'function') {","            callback = options;","            options  = {};","        }","","        for (i = 0, len = items.length; i < len; ++i) {","            item = items[i];","","            if (callback.call(this, item, i, this)) {","                filtered.push(item);","            }","        }","","        if (options.asList) {","            list = new this.constructor({model: this.model});","","            if (filtered.length) {","                list.add(filtered, {silent: true});","            }","","            return list;","        } else {","            return filtered;","        }","    },","","    /**","    If _name_ refers to an attribute on this ModelList instance, returns the","    value of that attribute. Otherwise, returns an array containing the values","    of the specified attribute from each model in this list.","","    @method get","    @param {String} name Attribute name or object property path.","    @return {Any|Array} Attribute value or array of attribute values.","    @see Model.get()","    **/","    get: function (name) {","        if (this.attrAdded(name)) {","            return AttrProto.get.apply(this, arguments);","        }","","        return this.invoke('get', name);","    },","","    /**","    If _name_ refers to an attribute on this ModelList instance, returns the","    HTML-escaped value of that attribute. Otherwise, returns an array containing","    the HTML-escaped values of the specified attribute from each model in this","    list.","","    The values are escaped using `Escape.html()`.","","    @method getAsHTML","    @param {String} name Attribute name or object property path.","    @return {String|String[]} HTML-escaped value or array of HTML-escaped","      values.","    @see Model.getAsHTML()","    **/","    getAsHTML: function (name) {","        if (this.attrAdded(name)) {","            return Y.Escape.html(AttrProto.get.apply(this, arguments));","        }","","        return this.invoke('getAsHTML', name);","    },","","    /**","    If _name_ refers to an attribute on this ModelList instance, returns the","    URL-encoded value of that attribute. Otherwise, returns an array containing","    the URL-encoded values of the specified attribute from each model in this","    list.","","    The values are encoded using the native `encodeURIComponent()` function.","","    @method getAsURL","    @param {String} name Attribute name or object property path.","    @return {String|String[]} URL-encoded value or array of URL-encoded values.","    @see Model.getAsURL()","    **/","    getAsURL: function (name) {","        if (this.attrAdded(name)) {","            return encodeURIComponent(AttrProto.get.apply(this, arguments));","        }","","        return this.invoke('getAsURL', name);","    },","","    /**","    Returns the model with the specified _clientId_, or `null` if not found.","","    @method getByClientId","    @param {String} clientId Client id.","    @return {Model} Model, or `null` if not found.","    **/","    getByClientId: function (clientId) {","        return this._clientIdMap[clientId] || null;","    },","","    /**","    Returns the model with the specified _id_, or `null` if not found.","","    Note that models aren't expected to have an id until they're saved, so if","    you're working with unsaved models, it may be safer to call","    `getByClientId()`.","","    @method getById","    @param {String|Number} id Model id.","    @return {Model} Model, or `null` if not found.","    **/","    getById: function (id) {","        return this._idMap[id] || null;","    },","","    /**","    Calls the named method on every model in the list. Any arguments provided","    after _name_ will be passed on to the invoked method.","","    @method invoke","    @param {String} name Name of the method to call on each model.","    @param {Any} [args*] Zero or more arguments to pass to the invoked method.","    @return {Array} Array of return values, indexed according to the index of","      the model on which the method was called.","    **/","    invoke: function (name /*, args* */) {","        var args = [this._items, name].concat(YArray(arguments, 1, true));","        return YArray.invoke.apply(YArray, args);","    },","","    /**","    Saves this model to the server.","","    This method delegates to the `sync()` method to perform the actual save","    operation, which is an asynchronous action. Specify a _callback_ function to","    be notified of success or failure.","","    A successful save operation will fire a `save` event, while an unsuccessful","    save operation will fire an `error` event with the `src` value \"save\".","","    If the save operation succeeds and one or more of the attributes returned in","    the server's response differ from this model's current attributes, a","    `change` event will be fired.","    @method save","    @param {Object} [options] Options to be passed to `sync()` and to `set()`","      when setting synced attributes. It's up to the custom sync implementation","      to determine what options it supports or requires, if any.","    @param {Function} [callback] Called when the sync operation finishes.","      @param {Error|null} callback.err If an error occurred or validation","        failed, this parameter will contain the error. If the sync operation","        succeeded, _err_ will be `null`.","      @param {Any} callback.response The server's response. This value will","        be passed to the `parse()` method, which is expected to parse it and","        return an attribute hash.","    @chainable","    **/","    save: function (options, callback) {","        var self = this;","","        // Allow callback as only arg.","        if (typeof options === 'function') {","            callback = options;","            options  = {};","        }","","        options || (options = {});","","        // self._validate(self.toJSON(), function (err) {","        //     if (err) {","        //         callback && callback.call(null, err);","        //         return;","        //     }","","        self.sync('create', options, function (err, response) {","                      var facade = {","                          options : options,","                          response: response","                      },","","                      parsed;","","                      if (err) {","                          facade.error = err;","                          facade.src   = 'save';","","                          self.fire(EVT_ERROR, facade);","                      } else {","                          // Lazy publish.","                          if (!self._saveEvent) {","                              self._saveEvent = self.publish(EVT_SAVE, {","                                                                 preventable: false","                                                             });","                          }","","                          if (response) {","                              parsed = facade.parsed = self._parse(response);","                              self.setAttrs(parsed, options);","                          }","","                          self.changed = {};","                          self.fire(EVT_SAVE, facade);","                      }","","                      callback && callback.apply(null, arguments);","                  });","        // });","","        return self;","    },","","    /**","    Returns the model at the specified _index_.","","    @method item","    @param {Number} index Index of the model to fetch.","    @return {Model} The model at the specified index, or `undefined` if there","      isn't a model there.","    **/","","    // item() is inherited from ArrayList.","","    /**","    Loads this list of models from the server.","","    This method delegates to the `sync()` method to perform the actual load","    operation, which is an asynchronous action. Specify a _callback_ function to","    be notified of success or failure.","","    If the load operation succeeds, a `reset` event will be fired.","","    @method load","    @param {Object} [options] Options to be passed to `sync()` and to","      `reset()` when adding the loaded models. It's up to the custom sync","      implementation to determine what options it supports or requires, if any.","    @param {Function} [callback] Called when the sync operation finishes.","      @param {Error} callback.err If an error occurred, this parameter will","        contain the error. If the sync operation succeeded, _err_ will be","        falsy.","      @param {Any} callback.response The server's response. This value will","        be passed to the `parse()` method, which is expected to parse it and","        return an array of model attribute hashes.","    @chainable","    **/","    load: function (options, callback) {","        var self = this;","","        // Allow callback as only arg.","        if (typeof options === 'function') {","            callback = options;","            options  = {};","        }","","        options || (options = {});","","        this.sync('read', options, function (err, response) {","            var facade = {","                    options : options,","                    response: response","                },","","                parsed;","","            if (err) {","                facade.error = err;","                facade.src   = 'load';","","                self.fire(EVT_ERROR, facade);","            } else {","                // Lazy publish.","                if (!self._loadEvent) {","                    self._loadEvent = self.publish(EVT_LOAD, {","                        preventable: false","                    });","                }","","                parsed = facade.parsed = self._parse(response);","","                self.reset(parsed, options);","                self.fire(EVT_LOAD, facade);","            }","","            callback && callback.apply(null, arguments);","        });","","        return this;","    },","","    /**","    Executes the specified function on each model in this list and returns an","    array of the function's collected return values.","","    @method map","    @param {Function} fn Function to execute on each model.","      @param {Model} fn.model Current model being iterated.","      @param {Number} fn.index Index of the current model in the list.","      @param {Model[]} fn.models Array of models being iterated.","    @param {Object} [thisObj] `this` object to use when calling _fn_.","    @return {Array} Array of return values from _fn_.","    **/","    map: function (fn, thisObj) {","        return YArray.map(this._items, fn, thisObj);","    },","","    /**","    Called to parse the _response_ when the list is loaded from the server.","    This method receives a server _response_ and is expected to return an array","    of model attribute hashes.","","    The default implementation assumes that _response_ is either an array of","    attribute hashes or a JSON string that can be parsed into an array of","    attribute hashes. If _response_ is a JSON string and either `Y.JSON` or the","    native `JSON` object are available, it will be parsed automatically. If a","    parse error occurs, an `error` event will be fired and the model will not be","    updated.","","    You may override this method to implement custom parsing logic if necessary.","","    @method parse","    @param {Any} response Server response.","    @return {Object[]} Array of model attribute hashes.","    **/","    parse: function (response) {","        if (typeof response === 'string') {","            try {","                return Y.JSON.parse(response) || [];","            } catch (ex) {","                this.fire(EVT_ERROR, {","                    error   : ex,","                    response: response,","                    src     : 'parse'","                });","","                return null;","            }","        }","","        return response || [];","    },","","    /**","    Removes the specified model or array of models from this list. You may also","    pass another ModelList instance to remove all the models that are in both","    that instance and this instance, or pass numerical indices to remove the","    models at those indices.","","    @method remove","    @param {Model|Model[]|ModelList|Number|Number[]} models Models or indices of","        models to remove.","    @param {Object} [options] Data to be mixed into the event facade of the","        `remove` event(s) for the removed models.","","        @param {Boolean} [options.silent=false] If `true`, no `remove` event(s)","            will be fired.","","    @return {Model|Model[]} Removed model or array of removed models.","    **/","    remove: function (models, options) {","        var isList = models._isYUIModelList;","","        if (isList || Lang.isArray(models)) {","            // We can't remove multiple models by index because the indices will","            // change as we remove them, so we need to get the actual models","            // first.","            models = YArray.map(isList ? models.toArray() : models, function (model) {","                if (Lang.isNumber(model)) {","                    return this.item(model);","                }","","                return model;","            }, this);","","            return YArray.map(models, function (model) {","                return this._remove(model, options);","            }, this);","        } else {","            return this._remove(models, options);","        }","    },","","    /**","    Completely replaces all models in the list with those specified, and fires a","    single `reset` event.","","    Use `reset` when you want to add or remove a large number of items at once","    with less overhead, and without firing `add` or `remove` events for each","    one.","","    @method reset","    @param {Model[]|ModelList|Object[]} [models] Models to add. May be existing","        model instances or hashes of model attributes, in which case new model","        instances will be created from the hashes. If a ModelList is passed, all","        the models in that list will be added to this list. Calling `reset()`","        without passing in any models will clear the list.","    @param {Object} [options] Data to be mixed into the event facade of the","        `reset` event.","","        @param {Boolean} [options.silent=false] If `true`, no `reset` event will","            be fired.","","    @chainable","    **/","    reset: function (models, options) {","        models  || (models  = []);","        options || (options = {});","","        var facade = Y.merge({src: 'reset'}, options);","","        if (models._isYUIModelList) {","            models = models.toArray();","        } else {","            models = YArray.map(models, function (model) {","                return model._isYUIModel ? model : new this.model(model);","            }, this);","        }","","        facade.models = models;","","        if (options.silent) {","            this._defResetFn(facade);","        } else {","            // Sort the models before firing the reset event.","            if (this.comparator) {","                models.sort(Y.bind(this._sort, this));","            }","","            this.fire(EVT_RESET, facade);","        }","","        return this;","    },","","    /**","    Executes the supplied function on each model in this list, and stops","    iterating if the callback returns `true`.","","    By default, the callback function's `this` object will refer to the model","    currently being iterated. Specify a `thisObj` to override the `this` object","    if desired.","","    Note: Iteration is performed on a copy of the internal array of models, so","    it's safe to delete a model from the list during iteration.","","    @method some","    @param {Function} callback Function to execute on each model.","        @param {Model} callback.model Model instance.","        @param {Number} callback.index Index of the current model.","        @param {ModelList} callback.list The ModelList being iterated.","    @param {Object} [thisObj] Object to use as the `this` object when executing","        the callback.","    @return {Boolean} `true` if the callback returned `true` for any item,","        `false` otherwise.","    @since 3.6.0","    **/","    some: function (callback, thisObj) {","        var items = this._items.concat(),","            i, item, len;","","        for (i = 0, len = items.length; i < len; i++) {","            item = items[i];","","            if (callback.call(thisObj || item, item, i, this)) {","                return true;","            }","        }","","        return false;","    },","","    /**","    Forcibly re-sorts the list.","","    Usually it shouldn't be necessary to call this method since the list","    maintains its sort order when items are added and removed, but if you change","    the `comparator` function after items are already in the list, you'll need","    to re-sort.","","    @method sort","    @param {Object} [options] Data to be mixed into the event facade of the","        `reset` event.","      @param {Boolean} [options.silent=false] If `true`, no `reset` event will","          be fired.","    @chainable","    **/","    sort: function (options) {","        if (!this.comparator) {","            return this;","        }","","        var models = this._items.concat(),","            facade;","","        options || (options = {});","","        models.sort(Y.bind(this._sort, this));","","        facade = Y.merge(options, {","            models: models,","            src   : 'sort'","        });","","        options.silent ? this._defResetFn(facade) :","                this.fire(EVT_RESET, facade);","","        return this;","    },","","    /**","    Override this method to provide a custom persistence implementation for this","    list. The default method just calls the callback without actually doing","    anything.","","    This method is called internally by `load()`.","","    @method sync","    @param {String} action Sync action to perform. May be one of the following:","","      * `create`: Store a list of newly-created models for the first time.","      * `delete`: Delete a list of existing models.","      * `read`  : Load a list of existing models.","      * `update`: Update a list of existing models.","","      Currently, model lists only make use of the `read` action, but other","      actions may be used in future versions.","","    @param {Object} [options] Sync options. It's up to the custom sync","      implementation to determine what options it supports or requires, if any.","    @param {Function} [callback] Called when the sync operation finishes.","      @param {Error} callback.err If an error occurred, this parameter will","        contain the error. If the sync operation succeeded, _err_ will be","        falsy.","      @param {Any} [callback.response] The server's response. This value will","        be passed to the `parse()` method, which is expected to parse it and","        return an array of model attribute hashes.","    **/","    sync: function (/* action, options, callback */) {","        var callback = YArray(arguments, 0, true).pop();","","        if (typeof callback === 'function') {","            callback();","        }","    },","","    /**","    Returns an array containing the models in this list.","","    @method toArray","    @return {Array} Array containing the models in this list.","    **/","    toArray: function () {","        return this._items.concat();","    },","","    /**","    Returns an array containing attribute hashes for each model in this list,","    suitable for being passed to `Y.JSON.stringify()`.","","    Under the hood, this method calls `toJSON()` on each model in the list and","    pushes the results into an array.","","    @method toJSON","    @return {Object[]} Array of model attribute hashes.","    @see Model.toJSON()","    **/","    toJSON: function () {","        return this.map(function (model) {","            return model.toJSON();","        });","    },","","    // -- Protected Methods ----------------------------------------------------","","    /**","    Adds the specified _model_ if it isn't already in this list.","","    If the model's `clientId` or `id` matches that of a model that's already in","    the list, an `error` event will be fired and the model will not be added.","","    @method _add","    @param {Model|Object} model Model or object to add.","    @param {Object} [options] Data to be mixed into the event facade of the","        `add` event for the added model.","      @param {Boolean} [options.silent=false] If `true`, no `add` event will be","          fired.","    @return {Model} The added model.","    @protected","    **/","    _add: function (model, options) {","        var facade, id;","","        options || (options = {});","","        if (!model._isYUIModel) {","            model = new this.model(model);","        }","","        id = model.get('id');","","        if (this._clientIdMap[model.get('clientId')]","                || (Lang.isValue(id) && this._idMap[id])) {","","            this.fire(EVT_ERROR, {","                error: 'Model is already in the list.',","                model: model,","                src  : 'add'","            });","","            return;","        }","","        facade = Y.merge(options, {","            index: 'index' in options ? options.index : this._findIndex(model),","            model: model","        });","","        options.silent ? this._defAddFn(facade) : this.fire(EVT_ADD, facade);","","        return model;","    },","","    /**","    Adds this list as a bubble target for the specified model's events.","","    @method _attachList","    @param {Model} model Model to attach to this list.","    @protected","    **/","    _attachList: function (model) {","        // Attach this list and make it a bubble target for the model.","        model.lists.push(this);","        model.addTarget(this);","    },","","    /**","    Clears all internal state and the internal list of models, returning this","    list to an empty state. Automatically detaches all models in the list.","","    @method _clear","    @protected","    **/","    _clear: function () {","        YArray.each(this._items, this._detachList, this);","","        this._clientIdMap = {};","        this._idMap       = {};","        this._items       = [];","    },","","    /**","    Compares the value _a_ to the value _b_ for sorting purposes. Values are","    assumed to be the result of calling a model's `comparator()` method. You can","    override this method to implement custom sorting logic, such as a descending","    sort or multi-field sorting.","","    @method _compare","    @param {Mixed} a First value to compare.","    @param {Mixed} b Second value to compare.","    @return {Number} `-1` if _a_ should come before _b_, `0` if they're","        equivalent, `1` if _a_ should come after _b_.","    @protected","    @since 3.5.0","    **/","    _compare: function (a, b) {","        return a < b ? -1 : (a > b ? 1 : 0);","    },","","    /**","    Removes this list as a bubble target for the specified model's events.","","    @method _detachList","    @param {Model} model Model to detach.","    @protected","    **/","    _detachList: function (model) {","        var index = YArray.indexOf(model.lists, this);","","        if (index > -1) {","            model.lists.splice(index, 1);","            model.removeTarget(this);","        }","    },","","    /**","    Returns the index at which the given _model_ should be inserted to maintain","    the sort order of the list.","","    @method _findIndex","    @param {Model} model The model being inserted.","    @return {Number} Index at which the model should be inserted.","    @protected","    **/","    _findIndex: function (model) {","        var items = this._items,","            max   = items.length,","            min   = 0,","            item, middle, needle;","","        if (!this.comparator || !max) {","            return max;","        }","","        needle = this.comparator(model);","","        // Perform an iterative binary search to determine the correct position","        // based on the return value of the `comparator` function.","        while (min < max) {","            middle = (min + max) >> 1; // Divide by two and discard remainder.","            item   = items[middle];","","            if (this._compare(this.comparator(item), needle) < 0) {","                min = middle + 1;","            } else {","                max = middle;","            }","        }","","        return min;","    },","","    /**","    Calls the public, overrideable `parse()` method and returns the result.","","    Override this method to provide a custom pre-parsing implementation. This","    provides a hook for custom persistence implementations to \"prep\" a response","    before calling the `parse()` method.","","    @method _parse","    @param {Any} response Server response.","    @return {Object[]} Array of model attribute hashes.","    @protected","    @see ModelList.parse()","    @since 3.7.0","    **/","    _parse: function (response) {","        return this.parse(response);","    },","","    /**","    Removes the specified _model_ if it's in this list.","","    @method _remove","    @param {Model|Number} model Model or index of the model to remove.","    @param {Object} [options] Data to be mixed into the event facade of the","        `remove` event for the removed model.","      @param {Boolean} [options.silent=false] If `true`, no `remove` event will","          be fired.","    @return {Model} Removed model.","    @protected","    **/","    _remove: function (model, options) {","        var index, facade;","","        options || (options = {});","","        if (Lang.isNumber(model)) {","            index = model;","            model = this.item(index);","        } else {","            index = this.indexOf(model);","        }","","        if (index === -1 || !model) {","            this.fire(EVT_ERROR, {","                error: 'Model is not in the list.',","                index: index,","                model: model,","                src  : 'remove'","            });","","            return;","        }","","        facade = Y.merge(options, {","            index: index,","            model: model","        });","","        options.silent ? this._defRemoveFn(facade) :","                this.fire(EVT_REMOVE, facade);","","        return model;","    },","","    /**","    Array sort function used by `sort()` to re-sort the models in the list.","","    @method _sort","    @param {Model} a First model to compare.","    @param {Model} b Second model to compare.","    @return {Number} `-1` if _a_ is less than _b_, `0` if equal, `1` if greater.","    @protected","    **/","    _sort: function (a, b) {","        return this._compare(this.comparator(a), this.comparator(b));","    },","","    // -- Event Handlers -------------------------------------------------------","","    /**","    Updates the model maps when a model's `id` attribute changes.","","    @method _afterIdChange","    @param {EventFacade} e","    @protected","    **/","    _afterIdChange: function (e) {","        var newVal  = e.newVal,","            prevVal = e.prevVal,","            target  = e.target;","","        if (Lang.isValue(prevVal)) {","            if (this._idMap[prevVal] === target) {","                delete this._idMap[prevVal];","            } else {","                // The model that changed isn't in this list. Probably just a","                // bubbled change event from a nested Model List.","                return;","            }","        } else {","            // The model had no previous id. Verify that it exists in this list","            // before continuing.","            if (this.indexOf(target) === -1) {","                return;","            }","        }","","        if (Lang.isValue(newVal)) {","            this._idMap[newVal] = target;","        }","    },","","    // -- Default Event Handlers -----------------------------------------------","","    /**","    Default event handler for `add` events.","","    @method _defAddFn","    @param {EventFacade} e","    @protected","    **/","    _defAddFn: function (e) {","        var model = e.model,","            id    = model.get('id');","","        this._clientIdMap[model.get('clientId')] = model;","","        if (Lang.isValue(id)) {","            this._idMap[id] = model;","        }","","        this._attachList(model);","        this._items.splice(e.index, 0, model);","    },","","    /**","    Default event handler for `remove` events.","","    @method _defRemoveFn","    @param {EventFacade} e","    @protected","    **/","    _defRemoveFn: function (e) {","        var model = e.model,","            id    = model.get('id');","","        this._detachList(model);","        delete this._clientIdMap[model.get('clientId')];","","        if (Lang.isValue(id)) {","            delete this._idMap[id];","        }","","        this._items.splice(e.index, 1);","    },","","    /**","    Default event handler for `reset` events.","","    @method _defResetFn","    @param {EventFacade} e","    @protected","    **/","    _defResetFn: function (e) {","        // When fired from the `sort` method, we don't need to clear the list or","        // add any models, since the existing models are sorted in place.","        if (e.src === 'sort') {","            this._items = e.models.concat();","            return;","        }","","        this._clear();","","        if (e.models.length) {","            this.add(e.models, {silent: true});","        }","    }","}, {","    NAME: 'modelListZope'","});","","Y.augment(ModelListZope, Y.ArrayList);","","}, '@VERSION@', {\"requires\": [\"array-extras\", \"array-invoke\", \"arraylist\", \"base-build\", \"escape\", \"json-parse\", \"model\"]});"];
_yuitest_coverage["build/model-list-zope/model-list-zope.js"].lines = {"1":0,"35":0,"147":0,"149":0,"150":0,"153":0,"190":0,"192":0,"194":0,"196":0,"198":0,"199":0,"203":0,"204":0,"205":0,"207":0,"209":0,"211":0,"212":0,"217":0,"263":0,"265":0,"266":0,"267":0,"272":0,"273":0,"278":0,"281":0,"328":0,"331":0,"332":0,"333":0,"336":0,"338":0,"339":0,"342":0,"346":0,"347":0,"348":0,"351":0,"376":0,"379":0,"380":0,"381":0,"384":0,"426":0,"431":0,"432":0,"433":0,"436":0,"437":0,"439":0,"440":0,"444":0,"445":0,"447":0,"448":0,"451":0,"453":0,"468":0,"469":0,"472":0,"490":0,"491":0,"494":0,"511":0,"512":0,"515":0,"526":0,"541":0,"555":0,"556":0,"586":0,"589":0,"590":0,"591":0,"594":0,"602":0,"603":0,"610":0,"611":0,"612":0,"614":0,"617":0,"618":0,"623":0,"624":0,"625":0,"628":0,"629":0,"632":0,"636":0,"673":0,"676":0,"677":0,"678":0,"681":0,"683":0,"684":0,"691":0,"692":0,"693":0,"695":0,"698":0,"699":0,"704":0,"706":0,"707":0,"710":0,"713":0,"729":0,"751":0,"752":0,"753":0,"755":0,"761":0,"765":0,"786":0,"788":0,"792":0,"793":0,"794":0,"797":0,"800":0,"801":0,"804":0,"831":0,"832":0,"834":0,"836":0,"837":0,"839":0,"840":0,"844":0,"846":0,"847":0,"850":0,"851":0,"854":0,"857":0,"883":0,"886":0,"887":0,"889":0,"890":0,"894":0,"913":0,"914":0,"917":0,"920":0,"922":0,"924":0,"929":0,"932":0,"964":0,"966":0,"967":0,"978":0,"993":0,"994":0,"1016":0,"1018":0,"1020":0,"1021":0,"1024":0,"1026":0,"1029":0,"1035":0,"1038":0,"1043":0,"1045":0,"1057":0,"1058":0,"1069":0,"1071":0,"1072":0,"1073":0,"1091":0,"1102":0,"1104":0,"1105":0,"1106":0,"1120":0,"1125":0,"1126":0,"1129":0,"1133":0,"1134":0,"1135":0,"1137":0,"1138":0,"1140":0,"1144":0,"1162":0,"1178":0,"1180":0,"1182":0,"1183":0,"1184":0,"1186":0,"1189":0,"1190":0,"1197":0,"1200":0,"1205":0,"1208":0,"1221":0,"1234":0,"1238":0,"1239":0,"1240":0,"1244":0,"1249":0,"1250":0,"1254":0,"1255":0,"1269":0,"1272":0,"1274":0,"1275":0,"1278":0,"1279":0,"1290":0,"1293":0,"1294":0,"1296":0,"1297":0,"1300":0,"1313":0,"1314":0,"1315":0,"1318":0,"1320":0,"1321":0,"1328":0};
_yuitest_coverage["build/model-list-zope/model-list-zope.js"].functions = {"ModelListZope:149":0,"initializer:189":0,"destructor:216":0,"(anonymous 2):266":0,"add:262":0,"(anonymous 3):346":0,"create:327":0,"each:375":0,"filter:425":0,"get:467":0,"getAsHTML:489":0,"getAsURL:510":0,"getByClientId:525":0,"getById:540":0,"invoke:554":0,"(anonymous 4):602":0,"save:585":0,"(anonymous 5):683":0,"load:672":0,"map:728":0,"parse:750":0,"(anonymous 6):792":0,"(anonymous 7):800":0,"remove:785":0,"(anonymous 8):839":0,"reset:830":0,"some:882":0,"sort:912":0,"sync:963":0,"toArray:977":0,"(anonymous 9):993":0,"toJSON:992":0,"_add:1015":0,"_attachList:1055":0,"_clear:1068":0,"_compare:1090":0,"_detachList:1101":0,"_findIndex:1119":0,"_parse:1161":0,"_remove:1177":0,"_sort:1220":0,"_afterIdChange:1233":0,"_defAddFn:1268":0,"_defRemoveFn:1289":0,"_defResetFn:1310":0,"(anonymous 1):1":0};
_yuitest_coverage["build/model-list-zope/model-list-zope.js"].coveredLines = 235;
_yuitest_coverage["build/model-list-zope/model-list-zope.js"].coveredFunctions = 46;
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1);
YUI.add('model-list-zope', function (Y, NAME) {

/**
Provides an API for managing an ordered list of Model instances.

@module app
@submodule model-list
@since 3.4.0
**/

/**
Provides an API for managing an ordered list of Model instances.

In addition to providing convenient `add`, `create`, `reset`, and `remove`
methods for managing the models in the list, ModelLists are also bubble targets
for events on the model instances they contain. This means, for example, that
you can add several models to a list, and then subscribe to the `*:change` event
on the list to be notified whenever any model in the list changes.

ModelLists also maintain sort order efficiently as models are added and removed,
based on a custom `comparator` function you may define (if no comparator is
defined, models are sorted in insertion order).

@class ModelList
@extends Base
@uses ArrayList
@constructor
@param {Object} config Config options.
    @param {Model|Model[]|ModelList|Object|Object[]} config.items Model
        instance, array of model instances, or ModelList to add to this list on
        init. The `add` event will not be fired for models added on init.
@since 3.4.0
**/

_yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 1)", 1);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 35);
var AttrProto = Y.Attribute.prototype,
    Lang      = Y.Lang,
    YArray    = Y.Array,

    /**
    Fired when a model is added to the list.

    Listen to the `on` phase of this event to be notified before a model is
    added to the list. Calling `e.preventDefault()` during the `on` phase will
    prevent the model from being added.

    Listen to the `after` phase of this event to be notified after a model has
    been added to the list.

    @event add
    @param {Model} model The model being added.
    @param {Number} index The index at which the model will be added.
    @preventable _defAddFn
    **/
    EVT_ADD = 'add',

    /**
    Fired when a model is created or updated via the `create()` method, but
    before the model is actually saved or added to the list. The `add` event
    will be fired after the model has been saved and added to the list.

    @event create
    @param {Model} model The model being created/updated.
    @since 3.5.0
    **/
    EVT_CREATE = 'create',

    /**
    Fired when an error occurs, such as when an attempt is made to add a
    duplicate model to the list, or when a sync layer response can't be parsed.

    @event error
    @param {Any} error Error message, object, or exception generated by the
      error. Calling `toString()` on this should result in a meaningful error
      message.
    @param {String} src Source of the error. May be one of the following (or any
      custom error source defined by a ModelList subclass):

      * `add`: Error while adding a model (probably because it's already in the
         list and can't be added again). The model in question will be provided
         as the `model` property on the event facade.
      * `parse`: An error parsing a JSON response. The response in question will
         be provided as the `response` property on the event facade.
      * `remove`: Error while removing a model (probably because it isn't in the
        list and can't be removed). The model in question will be provided as
        the `model` property on the event facade.
    **/
    EVT_ERROR = 'error',

    /**
    Fired after models are loaded from a sync layer.

    @event load
    @param {Object} parsed The parsed version of the sync layer's response to
        the load request.
    @param {Mixed} response The sync layer's raw, unparsed response to the load
        request.
    @since 3.5.0
    **/
    EVT_LOAD = 'load',

    /**
    Fired when a model is removed from the list.

    Listen to the `on` phase of this event to be notified before a model is
    removed from the list. Calling `e.preventDefault()` during the `on` phase
    will prevent the model from being removed.

    Listen to the `after` phase of this event to be notified after a model has
    been removed from the list.

    @event remove
    @param {Model} model The model being removed.
    @param {Number} index The index of the model being removed.
    @preventable _defRemoveFn
    **/
    EVT_REMOVE = 'remove',

    /**
    Fired when the list is completely reset via the `reset()` method or sorted
    via the `sort()` method.

    Listen to the `on` phase of this event to be notified before the list is
    reset. Calling `e.preventDefault()` during the `on` phase will prevent
    the list from being reset.

    Listen to the `after` phase of this event to be notified after the list has
    been reset.

    @event reset
    @param {Model[]} models Array of the list's new models after the reset.
    @param {String} src Source of the event. May be either `'reset'` or
      `'sort'`.
    @preventable _defResetFn
    **/
    EVT_RESET = 'reset';

    /**
    Fired after model attributes are saved to a sync layer.

    @event save
    @param {Object} [parsed] The parsed version of the sync layer's response to
        the save request, if there was a response.
    @param {any} [response] The sync layer's raw, unparsed response to the save
        request, if there was one.
    @since 3.5.0
    **/
    _yuitest_coverline("build/model-list-zope/model-list-zope.js", 147);
EVT_SAVE = 'save';

_yuitest_coverline("build/model-list-zope/model-list-zope.js", 149);
function ModelListZope() {
    _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "ModelListZope", 149);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 150);
ModelListZope.superclass.constructor.apply(this, arguments);
}

_yuitest_coverline("build/model-list-zope/model-list-zope.js", 153);
Y.ModelListZope = Y.extend(ModelListZope, Y.Base, {
    // -- Public Properties ----------------------------------------------------

    /**
    The `Model` class or subclass of the models in this list.

    The class specified here will be used to create model instances
    automatically based on attribute hashes passed to the `add()`, `create()`,
    and `reset()` methods.

    You may specify the class as an actual class reference or as a string that
    resolves to a class reference at runtime (the latter can be useful if the
    specified class will be loaded lazily).

    @property model
    @type Model|String
    @default Y.Model
    **/
    model: Y.Model,

    // -- Protected Properties -------------------------------------------------

    /**
    Total hack to allow us to identify ModelList instances without using
    `instanceof`, which won't work when the instance was created in another
    window or YUI sandbox.

    @property _isYUIModelList
    @type Boolean
    @default true
    @protected
    @since 3.5.0
    **/
    _isYUIModelList: true,

    // -- Lifecycle Methods ----------------------------------------------------
    initializer: function (config) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "initializer", 189);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 190);
config || (config = {});

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 192);
var model = this.model = config.model || this.model;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 194);
if (typeof model === 'string') {
            // Look for a namespaced Model class on `Y`.
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 196);
this.model = Y.Object.getValue(Y, model.split('.'));

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 198);
if (!this.model) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 199);
Y.error('ModelList: Model class not found: ' + model);
            }
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 203);
this.publish(EVT_ADD,    {defaultFn: this._defAddFn});
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 204);
this.publish(EVT_RESET,  {defaultFn: this._defResetFn});
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 205);
this.publish(EVT_REMOVE, {defaultFn: this._defRemoveFn});

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 207);
this.after('*:idChange', this._afterIdChange);

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 209);
this._clear();

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 211);
if (config.items) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 212);
this.add(config.items, {silent: true});
        }
    },

    destructor: function () {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "destructor", 216);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 217);
this._clear();
    },

    // -- Public Methods -------------------------------------------------------

    /**
    Adds the specified model or array of models to this list. You may also pass
    another ModelList instance, in which case all the models in that list will
    be added to this one as well.

    @example

        // Add a single model instance.
        list.add(new Model({foo: 'bar'}));

        // Add a single model, creating a new instance automatically.
        list.add({foo: 'bar'});

        // Add multiple models, creating new instances automatically.
        list.add([
            {foo: 'bar'},
            {baz: 'quux'}
        ]);

        // Add all the models in another ModelList instance.
        list.add(otherList);

    @method add
    @param {Model|Model[]|ModelList|Object|Object[]} models Model or array of
        models to add. May be existing model instances or hashes of model
        attributes, in which case new model instances will be created from the
        hashes. You may also pass a ModelList instance to add all the models it
        contains.
    @param {Object} [options] Data to be mixed into the event facade of the
        `add` event(s) for the added models.

        @param {Number} [options.index] Index at which to insert the added
            models. If not specified, the models will automatically be inserted
            in the appropriate place according to the current sort order as
            dictated by the `comparator()` method, if any.
        @param {Boolean} [options.silent=false] If `true`, no `add` event(s)
            will be fired.

    @return {Model|Model[]} Added model or array of added models.
    **/
    add: function (models, options) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "add", 262);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 263);
var isList = models._isYUIModelList;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 265);
if (isList || Lang.isArray(models)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 266);
return YArray.map(isList ? models.toArray() : models, function (model, index) {
                _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 2)", 266);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 267);
var modelOptions = options || {};

                // When an explicit insertion index is specified, ensure that
                // the index is increased by one for each subsequent item in the
                // array.
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 272);
if ('index' in modelOptions) {
                    _yuitest_coverline("build/model-list-zope/model-list-zope.js", 273);
modelOptions = Y.merge(modelOptions, {
                        index: modelOptions.index + index
                    });
                }

                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 278);
return this._add(model, modelOptions);
            }, this);
        } else {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 281);
return this._add(models, options);
        }
    },

    /**
    Define this method to provide a function that takes a model as a parameter
    and returns a value by which that model should be sorted relative to other
    models in this list.

    By default, no comparator is defined, meaning that models will not be sorted
    (they'll be stored in the order they're added).

    @example
        var list = new Y.ModelList({model: Y.Model});

        list.comparator = function (model) {
            return model.get('id'); // Sort models by id.
        };

    @method comparator
    @param {Model} model Model being sorted.
    @return {Number|String} Value by which the model should be sorted relative
      to other models in this list.
    **/

    // comparator is not defined by default

    /**
    Creates or updates the specified model on the server, then adds it to this
    list if the server indicates success.

    @method create
    @param {Model|Object} model Model to create. May be an existing model
      instance or a hash of model attributes, in which case a new model instance
      will be created from the hash.
    @param {Object} [options] Options to be passed to the model's `sync()` and
        `set()` methods and mixed into the `create` and `add` event facades.
      @param {Boolean} [options.silent=false] If `true`, no `add` event(s) will
          be fired.
    @param {Function} [callback] Called when the sync operation finishes.
      @param {Error} callback.err If an error occurred, this parameter will
        contain the error. If the sync operation succeeded, _err_ will be
        falsy.
      @param {Any} callback.response The server's response.
    @return {Model} Created model.
    **/
    create: function (model, options, callback) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "create", 327);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 328);
var self = this;

        // Allow callback as second arg.
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 331);
if (typeof options === 'function') {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 332);
callback = options;
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 333);
options  = {};
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 336);
options || (options = {});

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 338);
if (!model._isYUIModel) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 339);
model = new this.model(model);
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 342);
self.fire(EVT_CREATE, Y.merge(options, {
            model: model
        }));

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 346);
return model.save(options, function (err) {
            _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 3)", 346);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 347);
if (!err) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 348);
self.add(model, options);
            }

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 351);
callback && callback.apply(null, arguments);
        });
    },

    /**
    Executes the supplied function on each model in this list.

    By default, the callback function's `this` object will refer to the model
    currently being iterated. Specify a `thisObj` to override the `this` object
    if desired.

    Note: Iteration is performed on a copy of the internal array of models, so
    it's safe to delete a model from the list during iteration.

    @method each
    @param {Function} callback Function to execute on each model.
        @param {Model} callback.model Model instance.
        @param {Number} callback.index Index of the current model.
        @param {ModelList} callback.list The ModelList being iterated.
    @param {Object} [thisObj] Object to use as the `this` object when executing
        the callback.
    @chainable
    @since 3.6.0
    **/
    each: function (callback, thisObj) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "each", 375);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 376);
var items = this._items.concat(),
            i, item, len;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 379);
for (i = 0, len = items.length; i < len; i++) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 380);
item = items[i];
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 381);
callback.call(thisObj || item, item, i, this);
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 384);
return this;
    },

    /**
    Executes the supplied function on each model in this list. Returns an array
    containing the models for which the supplied function returned a truthy
    value.

    The callback function's `this` object will refer to this ModelList. Use
    `Y.bind()` to bind the `this` object to another object if desired.

    @example

        // Get an array containing only the models whose "enabled" attribute is
        // truthy.
        var filtered = list.filter(function (model) {
            return model.get('enabled');
        });

        // Get a new ModelList containing only the models whose "enabled"
        // attribute is truthy.
        var filteredList = list.filter({asList: true}, function (model) {
            return model.get('enabled');
        });

    @method filter
    @param {Object} [options] Filter options.
        @param {Boolean} [options.asList=false] If truthy, results will be
            returned as a new ModelList instance rather than as an array.

    @param {Function} callback Function to execute on each model.
        @param {Model} callback.model Model instance.
        @param {Number} callback.index Index of the current model.
        @param {ModelList} callback.list The ModelList being filtered.

    @return {Array|ModelList} Array of models for which the callback function
        returned a truthy value (empty if it never returned a truthy value). If
        the `options.asList` option is truthy, a new ModelList instance will be
        returned instead of an array.
    @since 3.5.0
    */
    filter: function (options, callback) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "filter", 425);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 426);
var filtered = [],
            items    = this._items,
            i, item, len, list;

        // Allow options as first arg.
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 431);
if (typeof options === 'function') {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 432);
callback = options;
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 433);
options  = {};
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 436);
for (i = 0, len = items.length; i < len; ++i) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 437);
item = items[i];

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 439);
if (callback.call(this, item, i, this)) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 440);
filtered.push(item);
            }
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 444);
if (options.asList) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 445);
list = new this.constructor({model: this.model});

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 447);
if (filtered.length) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 448);
list.add(filtered, {silent: true});
            }

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 451);
return list;
        } else {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 453);
return filtered;
        }
    },

    /**
    If _name_ refers to an attribute on this ModelList instance, returns the
    value of that attribute. Otherwise, returns an array containing the values
    of the specified attribute from each model in this list.

    @method get
    @param {String} name Attribute name or object property path.
    @return {Any|Array} Attribute value or array of attribute values.
    @see Model.get()
    **/
    get: function (name) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "get", 467);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 468);
if (this.attrAdded(name)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 469);
return AttrProto.get.apply(this, arguments);
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 472);
return this.invoke('get', name);
    },

    /**
    If _name_ refers to an attribute on this ModelList instance, returns the
    HTML-escaped value of that attribute. Otherwise, returns an array containing
    the HTML-escaped values of the specified attribute from each model in this
    list.

    The values are escaped using `Escape.html()`.

    @method getAsHTML
    @param {String} name Attribute name or object property path.
    @return {String|String[]} HTML-escaped value or array of HTML-escaped
      values.
    @see Model.getAsHTML()
    **/
    getAsHTML: function (name) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "getAsHTML", 489);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 490);
if (this.attrAdded(name)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 491);
return Y.Escape.html(AttrProto.get.apply(this, arguments));
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 494);
return this.invoke('getAsHTML', name);
    },

    /**
    If _name_ refers to an attribute on this ModelList instance, returns the
    URL-encoded value of that attribute. Otherwise, returns an array containing
    the URL-encoded values of the specified attribute from each model in this
    list.

    The values are encoded using the native `encodeURIComponent()` function.

    @method getAsURL
    @param {String} name Attribute name or object property path.
    @return {String|String[]} URL-encoded value or array of URL-encoded values.
    @see Model.getAsURL()
    **/
    getAsURL: function (name) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "getAsURL", 510);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 511);
if (this.attrAdded(name)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 512);
return encodeURIComponent(AttrProto.get.apply(this, arguments));
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 515);
return this.invoke('getAsURL', name);
    },

    /**
    Returns the model with the specified _clientId_, or `null` if not found.

    @method getByClientId
    @param {String} clientId Client id.
    @return {Model} Model, or `null` if not found.
    **/
    getByClientId: function (clientId) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "getByClientId", 525);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 526);
return this._clientIdMap[clientId] || null;
    },

    /**
    Returns the model with the specified _id_, or `null` if not found.

    Note that models aren't expected to have an id until they're saved, so if
    you're working with unsaved models, it may be safer to call
    `getByClientId()`.

    @method getById
    @param {String|Number} id Model id.
    @return {Model} Model, or `null` if not found.
    **/
    getById: function (id) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "getById", 540);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 541);
return this._idMap[id] || null;
    },

    /**
    Calls the named method on every model in the list. Any arguments provided
    after _name_ will be passed on to the invoked method.

    @method invoke
    @param {String} name Name of the method to call on each model.
    @param {Any} [args*] Zero or more arguments to pass to the invoked method.
    @return {Array} Array of return values, indexed according to the index of
      the model on which the method was called.
    **/
    invoke: function (name /*, args* */) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "invoke", 554);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 555);
var args = [this._items, name].concat(YArray(arguments, 1, true));
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 556);
return YArray.invoke.apply(YArray, args);
    },

    /**
    Saves this model to the server.

    This method delegates to the `sync()` method to perform the actual save
    operation, which is an asynchronous action. Specify a _callback_ function to
    be notified of success or failure.

    A successful save operation will fire a `save` event, while an unsuccessful
    save operation will fire an `error` event with the `src` value "save".

    If the save operation succeeds and one or more of the attributes returned in
    the server's response differ from this model's current attributes, a
    `change` event will be fired.
    @method save
    @param {Object} [options] Options to be passed to `sync()` and to `set()`
      when setting synced attributes. It's up to the custom sync implementation
      to determine what options it supports or requires, if any.
    @param {Function} [callback] Called when the sync operation finishes.
      @param {Error|null} callback.err If an error occurred or validation
        failed, this parameter will contain the error. If the sync operation
        succeeded, _err_ will be `null`.
      @param {Any} callback.response The server's response. This value will
        be passed to the `parse()` method, which is expected to parse it and
        return an attribute hash.
    @chainable
    **/
    save: function (options, callback) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "save", 585);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 586);
var self = this;

        // Allow callback as only arg.
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 589);
if (typeof options === 'function') {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 590);
callback = options;
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 591);
options  = {};
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 594);
options || (options = {});

        // self._validate(self.toJSON(), function (err) {
        //     if (err) {
        //         callback && callback.call(null, err);
        //         return;
        //     }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 602);
self.sync('create', options, function (err, response) {
                      _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 4)", 602);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 603);
var facade = {
                          options : options,
                          response: response
                      },

                      parsed;

                      _yuitest_coverline("build/model-list-zope/model-list-zope.js", 610);
if (err) {
                          _yuitest_coverline("build/model-list-zope/model-list-zope.js", 611);
facade.error = err;
                          _yuitest_coverline("build/model-list-zope/model-list-zope.js", 612);
facade.src   = 'save';

                          _yuitest_coverline("build/model-list-zope/model-list-zope.js", 614);
self.fire(EVT_ERROR, facade);
                      } else {
                          // Lazy publish.
                          _yuitest_coverline("build/model-list-zope/model-list-zope.js", 617);
if (!self._saveEvent) {
                              _yuitest_coverline("build/model-list-zope/model-list-zope.js", 618);
self._saveEvent = self.publish(EVT_SAVE, {
                                                                 preventable: false
                                                             });
                          }

                          _yuitest_coverline("build/model-list-zope/model-list-zope.js", 623);
if (response) {
                              _yuitest_coverline("build/model-list-zope/model-list-zope.js", 624);
parsed = facade.parsed = self._parse(response);
                              _yuitest_coverline("build/model-list-zope/model-list-zope.js", 625);
self.setAttrs(parsed, options);
                          }

                          _yuitest_coverline("build/model-list-zope/model-list-zope.js", 628);
self.changed = {};
                          _yuitest_coverline("build/model-list-zope/model-list-zope.js", 629);
self.fire(EVT_SAVE, facade);
                      }

                      _yuitest_coverline("build/model-list-zope/model-list-zope.js", 632);
callback && callback.apply(null, arguments);
                  });
        // });

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 636);
return self;
    },

    /**
    Returns the model at the specified _index_.

    @method item
    @param {Number} index Index of the model to fetch.
    @return {Model} The model at the specified index, or `undefined` if there
      isn't a model there.
    **/

    // item() is inherited from ArrayList.

    /**
    Loads this list of models from the server.

    This method delegates to the `sync()` method to perform the actual load
    operation, which is an asynchronous action. Specify a _callback_ function to
    be notified of success or failure.

    If the load operation succeeds, a `reset` event will be fired.

    @method load
    @param {Object} [options] Options to be passed to `sync()` and to
      `reset()` when adding the loaded models. It's up to the custom sync
      implementation to determine what options it supports or requires, if any.
    @param {Function} [callback] Called when the sync operation finishes.
      @param {Error} callback.err If an error occurred, this parameter will
        contain the error. If the sync operation succeeded, _err_ will be
        falsy.
      @param {Any} callback.response The server's response. This value will
        be passed to the `parse()` method, which is expected to parse it and
        return an array of model attribute hashes.
    @chainable
    **/
    load: function (options, callback) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "load", 672);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 673);
var self = this;

        // Allow callback as only arg.
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 676);
if (typeof options === 'function') {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 677);
callback = options;
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 678);
options  = {};
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 681);
options || (options = {});

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 683);
this.sync('read', options, function (err, response) {
            _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 5)", 683);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 684);
var facade = {
                    options : options,
                    response: response
                },

                parsed;

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 691);
if (err) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 692);
facade.error = err;
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 693);
facade.src   = 'load';

                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 695);
self.fire(EVT_ERROR, facade);
            } else {
                // Lazy publish.
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 698);
if (!self._loadEvent) {
                    _yuitest_coverline("build/model-list-zope/model-list-zope.js", 699);
self._loadEvent = self.publish(EVT_LOAD, {
                        preventable: false
                    });
                }

                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 704);
parsed = facade.parsed = self._parse(response);

                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 706);
self.reset(parsed, options);
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 707);
self.fire(EVT_LOAD, facade);
            }

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 710);
callback && callback.apply(null, arguments);
        });

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 713);
return this;
    },

    /**
    Executes the specified function on each model in this list and returns an
    array of the function's collected return values.

    @method map
    @param {Function} fn Function to execute on each model.
      @param {Model} fn.model Current model being iterated.
      @param {Number} fn.index Index of the current model in the list.
      @param {Model[]} fn.models Array of models being iterated.
    @param {Object} [thisObj] `this` object to use when calling _fn_.
    @return {Array} Array of return values from _fn_.
    **/
    map: function (fn, thisObj) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "map", 728);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 729);
return YArray.map(this._items, fn, thisObj);
    },

    /**
    Called to parse the _response_ when the list is loaded from the server.
    This method receives a server _response_ and is expected to return an array
    of model attribute hashes.

    The default implementation assumes that _response_ is either an array of
    attribute hashes or a JSON string that can be parsed into an array of
    attribute hashes. If _response_ is a JSON string and either `Y.JSON` or the
    native `JSON` object are available, it will be parsed automatically. If a
    parse error occurs, an `error` event will be fired and the model will not be
    updated.

    You may override this method to implement custom parsing logic if necessary.

    @method parse
    @param {Any} response Server response.
    @return {Object[]} Array of model attribute hashes.
    **/
    parse: function (response) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "parse", 750);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 751);
if (typeof response === 'string') {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 752);
try {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 753);
return Y.JSON.parse(response) || [];
            } catch (ex) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 755);
this.fire(EVT_ERROR, {
                    error   : ex,
                    response: response,
                    src     : 'parse'
                });

                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 761);
return null;
            }
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 765);
return response || [];
    },

    /**
    Removes the specified model or array of models from this list. You may also
    pass another ModelList instance to remove all the models that are in both
    that instance and this instance, or pass numerical indices to remove the
    models at those indices.

    @method remove
    @param {Model|Model[]|ModelList|Number|Number[]} models Models or indices of
        models to remove.
    @param {Object} [options] Data to be mixed into the event facade of the
        `remove` event(s) for the removed models.

        @param {Boolean} [options.silent=false] If `true`, no `remove` event(s)
            will be fired.

    @return {Model|Model[]} Removed model or array of removed models.
    **/
    remove: function (models, options) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "remove", 785);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 786);
var isList = models._isYUIModelList;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 788);
if (isList || Lang.isArray(models)) {
            // We can't remove multiple models by index because the indices will
            // change as we remove them, so we need to get the actual models
            // first.
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 792);
models = YArray.map(isList ? models.toArray() : models, function (model) {
                _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 6)", 792);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 793);
if (Lang.isNumber(model)) {
                    _yuitest_coverline("build/model-list-zope/model-list-zope.js", 794);
return this.item(model);
                }

                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 797);
return model;
            }, this);

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 800);
return YArray.map(models, function (model) {
                _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 7)", 800);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 801);
return this._remove(model, options);
            }, this);
        } else {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 804);
return this._remove(models, options);
        }
    },

    /**
    Completely replaces all models in the list with those specified, and fires a
    single `reset` event.

    Use `reset` when you want to add or remove a large number of items at once
    with less overhead, and without firing `add` or `remove` events for each
    one.

    @method reset
    @param {Model[]|ModelList|Object[]} [models] Models to add. May be existing
        model instances or hashes of model attributes, in which case new model
        instances will be created from the hashes. If a ModelList is passed, all
        the models in that list will be added to this list. Calling `reset()`
        without passing in any models will clear the list.
    @param {Object} [options] Data to be mixed into the event facade of the
        `reset` event.

        @param {Boolean} [options.silent=false] If `true`, no `reset` event will
            be fired.

    @chainable
    **/
    reset: function (models, options) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "reset", 830);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 831);
models  || (models  = []);
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 832);
options || (options = {});

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 834);
var facade = Y.merge({src: 'reset'}, options);

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 836);
if (models._isYUIModelList) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 837);
models = models.toArray();
        } else {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 839);
models = YArray.map(models, function (model) {
                _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 8)", 839);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 840);
return model._isYUIModel ? model : new this.model(model);
            }, this);
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 844);
facade.models = models;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 846);
if (options.silent) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 847);
this._defResetFn(facade);
        } else {
            // Sort the models before firing the reset event.
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 850);
if (this.comparator) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 851);
models.sort(Y.bind(this._sort, this));
            }

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 854);
this.fire(EVT_RESET, facade);
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 857);
return this;
    },

    /**
    Executes the supplied function on each model in this list, and stops
    iterating if the callback returns `true`.

    By default, the callback function's `this` object will refer to the model
    currently being iterated. Specify a `thisObj` to override the `this` object
    if desired.

    Note: Iteration is performed on a copy of the internal array of models, so
    it's safe to delete a model from the list during iteration.

    @method some
    @param {Function} callback Function to execute on each model.
        @param {Model} callback.model Model instance.
        @param {Number} callback.index Index of the current model.
        @param {ModelList} callback.list The ModelList being iterated.
    @param {Object} [thisObj] Object to use as the `this` object when executing
        the callback.
    @return {Boolean} `true` if the callback returned `true` for any item,
        `false` otherwise.
    @since 3.6.0
    **/
    some: function (callback, thisObj) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "some", 882);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 883);
var items = this._items.concat(),
            i, item, len;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 886);
for (i = 0, len = items.length; i < len; i++) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 887);
item = items[i];

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 889);
if (callback.call(thisObj || item, item, i, this)) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 890);
return true;
            }
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 894);
return false;
    },

    /**
    Forcibly re-sorts the list.

    Usually it shouldn't be necessary to call this method since the list
    maintains its sort order when items are added and removed, but if you change
    the `comparator` function after items are already in the list, you'll need
    to re-sort.

    @method sort
    @param {Object} [options] Data to be mixed into the event facade of the
        `reset` event.
      @param {Boolean} [options.silent=false] If `true`, no `reset` event will
          be fired.
    @chainable
    **/
    sort: function (options) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "sort", 912);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 913);
if (!this.comparator) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 914);
return this;
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 917);
var models = this._items.concat(),
            facade;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 920);
options || (options = {});

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 922);
models.sort(Y.bind(this._sort, this));

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 924);
facade = Y.merge(options, {
            models: models,
            src   : 'sort'
        });

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 929);
options.silent ? this._defResetFn(facade) :
                this.fire(EVT_RESET, facade);

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 932);
return this;
    },

    /**
    Override this method to provide a custom persistence implementation for this
    list. The default method just calls the callback without actually doing
    anything.

    This method is called internally by `load()`.

    @method sync
    @param {String} action Sync action to perform. May be one of the following:

      * `create`: Store a list of newly-created models for the first time.
      * `delete`: Delete a list of existing models.
      * `read`  : Load a list of existing models.
      * `update`: Update a list of existing models.

      Currently, model lists only make use of the `read` action, but other
      actions may be used in future versions.

    @param {Object} [options] Sync options. It's up to the custom sync
      implementation to determine what options it supports or requires, if any.
    @param {Function} [callback] Called when the sync operation finishes.
      @param {Error} callback.err If an error occurred, this parameter will
        contain the error. If the sync operation succeeded, _err_ will be
        falsy.
      @param {Any} [callback.response] The server's response. This value will
        be passed to the `parse()` method, which is expected to parse it and
        return an array of model attribute hashes.
    **/
    sync: function (/* action, options, callback */) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "sync", 963);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 964);
var callback = YArray(arguments, 0, true).pop();

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 966);
if (typeof callback === 'function') {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 967);
callback();
        }
    },

    /**
    Returns an array containing the models in this list.

    @method toArray
    @return {Array} Array containing the models in this list.
    **/
    toArray: function () {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "toArray", 977);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 978);
return this._items.concat();
    },

    /**
    Returns an array containing attribute hashes for each model in this list,
    suitable for being passed to `Y.JSON.stringify()`.

    Under the hood, this method calls `toJSON()` on each model in the list and
    pushes the results into an array.

    @method toJSON
    @return {Object[]} Array of model attribute hashes.
    @see Model.toJSON()
    **/
    toJSON: function () {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "toJSON", 992);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 993);
return this.map(function (model) {
            _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "(anonymous 9)", 993);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 994);
return model.toJSON();
        });
    },

    // -- Protected Methods ----------------------------------------------------

    /**
    Adds the specified _model_ if it isn't already in this list.

    If the model's `clientId` or `id` matches that of a model that's already in
    the list, an `error` event will be fired and the model will not be added.

    @method _add
    @param {Model|Object} model Model or object to add.
    @param {Object} [options] Data to be mixed into the event facade of the
        `add` event for the added model.
      @param {Boolean} [options.silent=false] If `true`, no `add` event will be
          fired.
    @return {Model} The added model.
    @protected
    **/
    _add: function (model, options) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_add", 1015);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1016);
var facade, id;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1018);
options || (options = {});

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1020);
if (!model._isYUIModel) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1021);
model = new this.model(model);
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1024);
id = model.get('id');

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1026);
if (this._clientIdMap[model.get('clientId')]
                || (Lang.isValue(id) && this._idMap[id])) {

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1029);
this.fire(EVT_ERROR, {
                error: 'Model is already in the list.',
                model: model,
                src  : 'add'
            });

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1035);
return;
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1038);
facade = Y.merge(options, {
            index: 'index' in options ? options.index : this._findIndex(model),
            model: model
        });

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1043);
options.silent ? this._defAddFn(facade) : this.fire(EVT_ADD, facade);

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1045);
return model;
    },

    /**
    Adds this list as a bubble target for the specified model's events.

    @method _attachList
    @param {Model} model Model to attach to this list.
    @protected
    **/
    _attachList: function (model) {
        // Attach this list and make it a bubble target for the model.
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_attachList", 1055);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1057);
model.lists.push(this);
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1058);
model.addTarget(this);
    },

    /**
    Clears all internal state and the internal list of models, returning this
    list to an empty state. Automatically detaches all models in the list.

    @method _clear
    @protected
    **/
    _clear: function () {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_clear", 1068);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1069);
YArray.each(this._items, this._detachList, this);

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1071);
this._clientIdMap = {};
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1072);
this._idMap       = {};
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1073);
this._items       = [];
    },

    /**
    Compares the value _a_ to the value _b_ for sorting purposes. Values are
    assumed to be the result of calling a model's `comparator()` method. You can
    override this method to implement custom sorting logic, such as a descending
    sort or multi-field sorting.

    @method _compare
    @param {Mixed} a First value to compare.
    @param {Mixed} b Second value to compare.
    @return {Number} `-1` if _a_ should come before _b_, `0` if they're
        equivalent, `1` if _a_ should come after _b_.
    @protected
    @since 3.5.0
    **/
    _compare: function (a, b) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_compare", 1090);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1091);
return a < b ? -1 : (a > b ? 1 : 0);
    },

    /**
    Removes this list as a bubble target for the specified model's events.

    @method _detachList
    @param {Model} model Model to detach.
    @protected
    **/
    _detachList: function (model) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_detachList", 1101);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1102);
var index = YArray.indexOf(model.lists, this);

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1104);
if (index > -1) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1105);
model.lists.splice(index, 1);
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1106);
model.removeTarget(this);
        }
    },

    /**
    Returns the index at which the given _model_ should be inserted to maintain
    the sort order of the list.

    @method _findIndex
    @param {Model} model The model being inserted.
    @return {Number} Index at which the model should be inserted.
    @protected
    **/
    _findIndex: function (model) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_findIndex", 1119);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1120);
var items = this._items,
            max   = items.length,
            min   = 0,
            item, middle, needle;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1125);
if (!this.comparator || !max) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1126);
return max;
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1129);
needle = this.comparator(model);

        // Perform an iterative binary search to determine the correct position
        // based on the return value of the `comparator` function.
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1133);
while (min < max) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1134);
middle = (min + max) >> 1; // Divide by two and discard remainder.
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1135);
item   = items[middle];

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1137);
if (this._compare(this.comparator(item), needle) < 0) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1138);
min = middle + 1;
            } else {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1140);
max = middle;
            }
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1144);
return min;
    },

    /**
    Calls the public, overrideable `parse()` method and returns the result.

    Override this method to provide a custom pre-parsing implementation. This
    provides a hook for custom persistence implementations to "prep" a response
    before calling the `parse()` method.

    @method _parse
    @param {Any} response Server response.
    @return {Object[]} Array of model attribute hashes.
    @protected
    @see ModelList.parse()
    @since 3.7.0
    **/
    _parse: function (response) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_parse", 1161);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1162);
return this.parse(response);
    },

    /**
    Removes the specified _model_ if it's in this list.

    @method _remove
    @param {Model|Number} model Model or index of the model to remove.
    @param {Object} [options] Data to be mixed into the event facade of the
        `remove` event for the removed model.
      @param {Boolean} [options.silent=false] If `true`, no `remove` event will
          be fired.
    @return {Model} Removed model.
    @protected
    **/
    _remove: function (model, options) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_remove", 1177);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1178);
var index, facade;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1180);
options || (options = {});

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1182);
if (Lang.isNumber(model)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1183);
index = model;
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1184);
model = this.item(index);
        } else {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1186);
index = this.indexOf(model);
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1189);
if (index === -1 || !model) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1190);
this.fire(EVT_ERROR, {
                error: 'Model is not in the list.',
                index: index,
                model: model,
                src  : 'remove'
            });

            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1197);
return;
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1200);
facade = Y.merge(options, {
            index: index,
            model: model
        });

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1205);
options.silent ? this._defRemoveFn(facade) :
                this.fire(EVT_REMOVE, facade);

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1208);
return model;
    },

    /**
    Array sort function used by `sort()` to re-sort the models in the list.

    @method _sort
    @param {Model} a First model to compare.
    @param {Model} b Second model to compare.
    @return {Number} `-1` if _a_ is less than _b_, `0` if equal, `1` if greater.
    @protected
    **/
    _sort: function (a, b) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_sort", 1220);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1221);
return this._compare(this.comparator(a), this.comparator(b));
    },

    // -- Event Handlers -------------------------------------------------------

    /**
    Updates the model maps when a model's `id` attribute changes.

    @method _afterIdChange
    @param {EventFacade} e
    @protected
    **/
    _afterIdChange: function (e) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_afterIdChange", 1233);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1234);
var newVal  = e.newVal,
            prevVal = e.prevVal,
            target  = e.target;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1238);
if (Lang.isValue(prevVal)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1239);
if (this._idMap[prevVal] === target) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1240);
delete this._idMap[prevVal];
            } else {
                // The model that changed isn't in this list. Probably just a
                // bubbled change event from a nested Model List.
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1244);
return;
            }
        } else {
            // The model had no previous id. Verify that it exists in this list
            // before continuing.
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1249);
if (this.indexOf(target) === -1) {
                _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1250);
return;
            }
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1254);
if (Lang.isValue(newVal)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1255);
this._idMap[newVal] = target;
        }
    },

    // -- Default Event Handlers -----------------------------------------------

    /**
    Default event handler for `add` events.

    @method _defAddFn
    @param {EventFacade} e
    @protected
    **/
    _defAddFn: function (e) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_defAddFn", 1268);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1269);
var model = e.model,
            id    = model.get('id');

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1272);
this._clientIdMap[model.get('clientId')] = model;

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1274);
if (Lang.isValue(id)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1275);
this._idMap[id] = model;
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1278);
this._attachList(model);
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1279);
this._items.splice(e.index, 0, model);
    },

    /**
    Default event handler for `remove` events.

    @method _defRemoveFn
    @param {EventFacade} e
    @protected
    **/
    _defRemoveFn: function (e) {
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_defRemoveFn", 1289);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1290);
var model = e.model,
            id    = model.get('id');

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1293);
this._detachList(model);
        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1294);
delete this._clientIdMap[model.get('clientId')];

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1296);
if (Lang.isValue(id)) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1297);
delete this._idMap[id];
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1300);
this._items.splice(e.index, 1);
    },

    /**
    Default event handler for `reset` events.

    @method _defResetFn
    @param {EventFacade} e
    @protected
    **/
    _defResetFn: function (e) {
        // When fired from the `sort` method, we don't need to clear the list or
        // add any models, since the existing models are sorted in place.
        _yuitest_coverfunc("build/model-list-zope/model-list-zope.js", "_defResetFn", 1310);
_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1313);
if (e.src === 'sort') {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1314);
this._items = e.models.concat();
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1315);
return;
        }

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1318);
this._clear();

        _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1320);
if (e.models.length) {
            _yuitest_coverline("build/model-list-zope/model-list-zope.js", 1321);
this.add(e.models, {silent: true});
        }
    }
}, {
    NAME: 'modelListZope'
});

_yuitest_coverline("build/model-list-zope/model-list-zope.js", 1328);
Y.augment(ModelListZope, Y.ArrayList);

}, '@VERSION@', {"requires": ["array-extras", "array-invoke", "arraylist", "base-build", "escape", "json-parse", "model"]});
